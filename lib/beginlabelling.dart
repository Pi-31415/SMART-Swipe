import 'package:flutter/material.dart';

class BeginLabellingPage extends StatelessWidget {
  const BeginLabellingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Begin Labelling'),
    );
  }
}
